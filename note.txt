git init
git add
git rm --cached index.html
git commit -m "Your comment here"


(use --cached to keep the file, or -f to force removal)

rm -f คือลบไฟล์เลย


After you use the command git commit -m 'Initial Commit' in Page 18, if you use the command 'cat .\.git\COMMIT_EDITMSG', what will be shown on the screen?

Initial Commit
