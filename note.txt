git init
git add
git rm --cached index.html
git commit -m "Your comment here"


(use --cached to keep the file, or -f to force removal)

rm -f คือลบไฟล์เลย


After you use the command git commit -m 'Initial Commit' in Page 18, if you use the command 'cat .\.git\COMMIT_EDITMSG', what will be shown on the screen?

Initial Commit


git config --global user.email "6833150921@student.chula.ac.th"
git config --global user.name : "New"

git log
git log --online


git checkout [id]
○ Have a look at a version

git checkout master
○ Go back to master branch

git revert [id]
○ Go back to [id] version and add another step to the log.

git reset [id]
○ Undo reset
■ git add .
■ git commit -m “your comment”

git reset [id] –hard
○ Cannot undo this reset


git branch -m ..
//rename branch name in branch we are

git branch -m master main
//rename branch master to branch main

//m stand for move

git branch --set-upstream-to=origin/main main
//for -u 